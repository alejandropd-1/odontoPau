/**
 * Funcionalidades que permanecen implementadas pero no se muestran actualmente.
 * Cambiar `testimonials` a `true` reactiva la seccion y su enlace de navegacion.
 */
export const siteFeatures = {
  testimonials: false,
} as const;
